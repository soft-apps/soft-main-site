import React from "react";
import BigIcon from "./BigIcon";

import "../styles/FlightSimulatorIcon.scss";

const FlightSimulatorIcon = props => {
  return <BigIcon {...props} />;
};

export default FlightSimulatorIcon;
